---
id: T-004
title: 120 个事件 + 掉落表 + 随机幸存者模板
owner: codex
status: todo
depends: [T-003]
---

## 要做
- `src/content/events/<locationId>.ts` 每地点一个文件，`src/content/events/index.ts` 合并导出 `events: EventDef[]`。
- 序章 30：家 6 / 公司 3 / 超市 5 / 药店 3 / 五金 4 / 黑市 3 / 银行 4 / 军区门口 2。搞钱手段按 DESIGN.md 4.1 全部覆盖（打工、卖房、贷款、炒股、彩票）。
- 末日后 90：基地内 20（建造/训练/看书/守卫/宠物/伙伴对话）、超市废墟 8、医院 8、军械库 6、居民楼 10、工厂 6、加油站 6、农场 8、军区基地 8、黑鸦地盘 6、随机遭遇 4（丧尸/幸存者/势力，`locationId` 留空字符串由引擎注入）。
- 用 `conditions` 的 `phase` / `weeksBeforeEnd` / `month` 限定时段，保证 **每周至少 5 个可见事件**。随机事件 `weight` 1~20，剧情事件 `weight: 0`。
- 检定事件的 `outcomes` 写 fail/common/fine/rare；无检定事件只写 `fine`。
- 末日后每月至少 1 个"剧情解"事件（`resolvesCrisis` + effect `resolveCrisis`），对应 DESIGN.md 第 8 节的记忆表。
- 掉落表 `src/content/loot.ts`：每个外出地点 1 张 + 丧尸击杀 1 张（含晶核）。
- 随机幸存者：`src/content/survivors.ts`，特质 15 条（`SurvivorTraitDef`）+ 名字池 60 个。
- 文案要求：写实、有细节、不解释机制；主角第一人称内心 OS 放 `text`，结果放 `outcomes.*.text`。

## 验收
`npm run build` 通过；引用的 cardId / npcId / flag 都存在。
