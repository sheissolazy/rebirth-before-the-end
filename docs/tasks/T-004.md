---
id: T-004
title: 80 个事件内容（按地点分文件）
owner: codex
status: todo
depends: [T-003]
---

## 要做
- `src/content/events/<locationId>.ts` 每地点一个文件，`src/content/events/index.ts` 合并导出 `events: EventDef[]`。
- 数量：家 12 / 公司 6 / 超市 10 / 药店 8 / 五金 8 / 二手市场 8 / 小区群 10 / 快递站 6 / 医院 6 / 农场 6。
- 每个事件用 `conditions` 里的 `month` 限定出现时段，保证 **每个月至少 5 个可见事件**。
- 检定事件的 `outcomes` 四档都写；无检定事件只写 `silver`。
- 每月至少 1 个"剧情解"事件（effect `resolveCrisis`），对应 DESIGN.md 3.1 的记忆表。
- 文案要求：写实、有细节、不解释机制；主角第一人称内心 OS 放 `text`，结果放 `outcomes.*.text`。

## 验收
`npm run build` 通过；引用的 cardId / npcId / flag 都存在。
