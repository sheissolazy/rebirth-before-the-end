---
id: T-003
title: 40 种物资卡 + 16 张危机卡 + 麻烦/情报卡内容
owner: codex
status: todo
depends: [T-001]
---

## 要做
- `src/content/cards.ts`，导出 `cards: CardDef[]`，替换 `index.ts` 里的 `sampleCards`。
- 物资 40 种：六维度各 6~8 种，四档都要有；食物/药品填 `shelfLifeWeeks`；`size` 1~3；`basePrice` 按第 1 年 1 月现实价格。参考 `cards.sample.ts` 格式。
- 危机 16 张：`crisis_<kind>_<tier>`，每张 `onDraw / onSurvive / onFail` 三段文案（写实、口语、末日日记感，每段 1~3 句）。
- 麻烦卡 10 张、情报卡 5 张、5 位 NPC 的技能卡 5 张、心情卡 5 张。
- id 唯一；`resolvedByEventIds` 引用的事件先在 T-004 里对上。

## 验收
`npm run build` 通过；每个维度四档齐全。
