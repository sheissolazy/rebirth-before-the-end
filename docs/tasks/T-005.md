---
id: T-005
title: 三条恋爱线（阿哲 / 林晚舟 / 老K）各 8 个事件
owner: codex
status: todo
depends: [T-004]
---

## 要做
- `src/content/events/story_<npcId>.ts`，每人 8 个 `once: true` 事件，`storylineNpcId` 标记。
- 节奏：1~2 月相识（熟人）→ 3~5 月（朋友，解锁技能卡 `gainCard`）→ 6~8 月（暧昧，出现 `npcCohabit` 邀请）→ 9~11 月（恋人）→ 12 月末日降临时的专属片段。
- 每条线至少 1 个"他可能死"的分支（`npcInjure` / `npcDie`），触发条件写实（例如林晚舟在 10 月封控期间超负荷）。
- 多线：写 1 个修罗场事件 `ev_home_confrontation`，条件为两人同时 ≥ crush，检定 charm+mind。
- 结局：`src/content/endings.ts` 写 6 个 `EndingDef`（死亡 / 独活 / 双人×3 / 多人 / 社区，判定顺序按 DESIGN.md 3.9）。

## 验收
`npm run build` 通过；每条线 8 个事件 id 连续、条件递进不矛盾。
