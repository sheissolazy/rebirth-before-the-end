---
id: T-005
title: 男主线每人扩到 10 个事件（现有 story.ts）
owner: codex
status: todo
depends: [T-004]
---

## 要做
- 现有 `src/content/events/story.ts`（顾沉 4 / 沈砚 4 / 谢临 4 / 江野 2 / 阿寂 1，结局 7 个已在 `endings.ts`）。按人拆成 `story_<npcId>.ts`，每人补到 10 个 `once: true` 事件，`storylineNpcId` 标记，人设按 DESIGN.md 10.3。
- 节奏：序章 1~2 个（他不信你 / 谢临：两个重生者互相试探）→ 1~3 月相识 → 4~6 月朋友（`gainCard` 技能卡）→ 7~9 月暧昧（`npcJoin` 入住）→ 10~11 月恋人 → 12 月极寒尸潮时的专属片段。
- 沈砚线要埋"5 种疫苗材料"的收集 flag；谢临线要有 1 次"他知道你不知道的事"的记忆修正（`revealCrisis`）。
- 每条线至少 1 个"他可能死"的分支（`injure` / `kill`），触发条件写实。
- 多线：写 1 个修罗场事件 `ev_home_confrontation`，条件为两人同时 ≥ crush，检定 charm+mind，大成功设 flag `harmony`。
- 结局：`src/content/endings.ts` 写 7 个 `EndingDef`（死亡 / 独活 / 双人×3 / 多人 / 军区收编），判定顺序按数组顺序，每个填 `rebirthPoints`。

## 验收
`npm run build` 通过；每条线 8 个事件 id 连续、条件递进不矛盾。
